
const IpList = () => {


	return (
  <>
    <h2>Lista de IPs:</h2>
{/*     <ul>
        {ipList.map((ipData, index) => (
          <li key={index}>
            <p><strong>IP:</strong> {ipData.ip}</p>
            <p><strong>Date:</strong> {ipData.date}</p>
            <p><strong>Hour:</strong> {ipData.hour}</p>
          </li>
        ))}
      </ul> */}
  </>
  );
};

export default IpList;
